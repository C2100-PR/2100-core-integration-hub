apiVersion: v1
kind: AgentHubConfig
metadata:
  name: agent-hub-core
  version: "1.0"
spec:
  agentTypes:
    - name: "DrLucy"
      endpoint: ${DR_LUCY_ENDPOINT}
      variants: ["standard", "enterprise"]...